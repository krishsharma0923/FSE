name: Hello Workflow

on: [push]

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello
        run: echo "Hello World"

      - name: Print Name
        run: echo "My name is Krish Sharma"
